# Refactored initialization code

# Example content: Adjusted initialization process using a loop

initializations = [
    {'name': 'init1', 'value': 10},
    {'name': 'init2', 'value': 20},
    {'name': 'init3', 'value': 30},
]

for init in initializations:
    print(f'Initializing {init['name']} with value {init['value']}')
